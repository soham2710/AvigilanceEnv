# Avigilance 2.0 — Python dependencies
# Environment server and inference baseline
fastapi==0.115.2
gradio>=6.11.0,<7.0.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
openai>=2.7.2
scipy==1.13.0
numpy==1.26.4
python-dotenv==1.0.0
httpx==0.27.0

# Deep learning — required for pytorch_agent.py, train.py, train_ppo.py
# inference.py does NOT use torch; it calls a remote LLM API.
torch>=2.0.0
